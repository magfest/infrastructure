__: merge-first

reggie:
  db:
    password: '{{ salt["random.get_str"](14) }}'

  queue:
    password: '{{ salt["random.get_str"](14) }}'

  sessions:
    password: '{{ salt["random.get_str"](14) }}'
