[Unit]
Description=Legacy magbot (based on hubot)
After=network.target

[Service]
Restart=always
User=magbot
ExecStart=/bin/bash -c "/srv/legacy_magbot/run-bot.sh >> /var/log/hubot.out.log 2>> /var/log/hubot.err.log"

[Install]
WantedBy=multi-user.target
